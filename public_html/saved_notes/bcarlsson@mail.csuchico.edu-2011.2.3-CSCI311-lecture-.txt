<div>Deletes in BSTs<br><br><ul><li dir="ltr">replace the deleted node with its in-order predecessor (rightmost node of the node's left subtree)<br></li><li dir="ltr">when deleting from AVL trees, we must recheck the balance condition at each node on the path from the deleted node to the root</li></ul></div><div><font class="Apple-style-span" size="4"><br></font></div><font class="Apple-style-span" size="4">Iterative vs Recursive<br></font><br>Given an array of unsigned values, find the sum of the first <span style="font-family: monospace;">n</span> elements in the array (1) iteratively and (2) recursively.<br><br><span style="font-size: large;">Iterative: O(n)</span><span style="font-family: monospace;">unsigned sum(unsigned A[], unsigned n) {<br><br>	unsigned sumA = 0;<br><br>	for(unsigned i = 0; i &lt; n; i++)<br>		sumA += A[i];<br><br>	return sumA;<br><br>}</span><span style="font-size: large;">Recursive: O(n)<br></span><span style="font-family: monospace;">unsigned sum(unsigned A[], unsigned n) {<br><br>	// base case<br>	if(n == 0)<br>		return 0;<br><br>	return A[n-1] + sum(A, n-1);<br><br>}</span><br>


